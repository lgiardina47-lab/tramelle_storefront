/** Segmenti sotto /user/reviews (link relativi, prefisso `/${locale}` in NavigationItem). */
export const reviewSubNav = [
  { href: "/user/reviews", labelKey: "reviewsNavToWrite" as const },
  { href: "/user/reviews/written", labelKey: "reviewsNavWritten" as const },
]
