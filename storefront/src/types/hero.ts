/** Slide cover rotative hero (condiviso API + componenti). */
export type HeroCoverSlide = {
  src: string
  alt: string
  handle?: string
  displayName?: string
  locationLine?: string
  logoSrc?: string
  catalogIndex1Based?: number
}
