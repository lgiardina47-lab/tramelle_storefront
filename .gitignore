# Chiavi SSH per il client: mai in git
ssh-client-download/

# Compose: interpolazione build args per storefront/vendor (stessi valori di storefront/.env se preferisci copiare)
.env
backend/.env
storefront/.env.local
storefront/.env.production.local
admin-panel/.env
admin-panel/.env.production.local
vendor-panel/.env
vendor-panel/.env.production.local
**/node_modules
**/.medusa

# Export categorie (generato da `medusa exec export-product-categories`)
/catalog-structure.json

# Dati scraping venditori: solo macchina locale, mai in remoto
dati_venditori/

# Token GitHub / secret locali (mai in remoto)
storefront/token.txt
**/token.txt

# Python bytecode
__pycache__/
*.py[cod]
